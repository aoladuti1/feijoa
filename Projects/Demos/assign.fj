
// Hello
/* The comments are...
	working! */
	
x = 10+50;
b = 60;
c = (b + x + (60 + 20));

//a; would be invalid syntax as declarations don't exist in this language.

sysln!(c + 70); // prints 270
sysout!(12); sysout!(34); // prints 1234