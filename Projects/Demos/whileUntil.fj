sysline("This program prints numbers 1 to 10 in ascending order,
and then in descending order, using 'while' and 'until' loops\n");

x = 0;

until (x == 10) {
	x += 1;
	sysline(x);
}

sysline("_______________");

while (x > 0) {
	sysline(x);
	x -= 1;
}
